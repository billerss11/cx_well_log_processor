# Cross-surface tests

Python unit and contract tests live under `python/tests`.

This directory is reserved for fixtures, golden files, integration tests,
performance tests, and desktop end-to-end tests that span more than one
workspace package.
