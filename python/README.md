# Well Log Engine

Python application engine, local HTTP API, and CLI for the offline well-log
processor. Install this package from the repository root with:

```powershell
python -m pip install -e "./python[dev]"
```
